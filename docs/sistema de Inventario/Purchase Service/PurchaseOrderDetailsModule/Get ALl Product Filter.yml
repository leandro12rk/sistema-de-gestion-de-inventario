info:
  name: Get ALl Product Filter
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/purchase/purchase-orders"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
