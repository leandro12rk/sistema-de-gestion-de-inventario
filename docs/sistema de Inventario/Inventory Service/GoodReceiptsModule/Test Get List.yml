info:
  name: Test Get List
  type: http
  seq: 5

http:
  method: GET
  url: "{{URL}}/products/dashboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
