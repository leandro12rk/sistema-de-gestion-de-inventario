info:
  name: Get All Product Data
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/products"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
