info:
  name: Get Dashboard Data
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/products/dashboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
